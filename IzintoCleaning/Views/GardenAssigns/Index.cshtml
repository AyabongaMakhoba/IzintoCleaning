@model IEnumerable<IzintoCleaning.Models.GardenAssign>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EmpID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Order_ID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.EmpID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Order_ID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GardenOrderId }) |
            @Html.ActionLink("Details", "Details", new { id=item.GardenOrderId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GardenOrderId })
        </td>
    </tr>
}

</table>
