@model IEnumerable<IzintoCleaning.Models.Service>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.admin.AdminName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SerName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SerWage)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SerBasic)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.admin.AdminName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SerName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SerWage)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SerBasic)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SerCode }) |
            @Html.ActionLink("Details", "Details", new { id=item.SerCode }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SerCode })
        </td>
    </tr>
}

</table>
