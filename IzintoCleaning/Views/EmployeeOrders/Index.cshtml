@model IEnumerable<IzintoCleaning.Models.EmployeeOrder>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Order_ID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EmpID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Order_ID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EmpID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EmployeeOrderId }) |
            @Html.ActionLink("Details", "Details", new { id=item.EmployeeOrderId }) |
            @Html.ActionLink("Cancel", "Delete", new { id=item.EmployeeOrderId })
        </td>
    </tr>
}

</table>
